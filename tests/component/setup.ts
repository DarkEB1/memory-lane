// @testing-library/react-native v13 auto-extends jest matchers on import of the
// library; there is no separate extend-expect entry point to load here. This
// file remains as the component project's setupFilesAfterEnv hook for any future
// global test configuration.
export {};
